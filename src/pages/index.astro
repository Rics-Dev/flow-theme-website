---
import Layout from '../layouts/Layout.astro';
import ColorPalette from '../components/ColorPalette.astro';
import PlatformShowcase from '../components/PlatformShowcase.astro';
---

<Layout title="Home">
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <h1>Flow Theme</h1>
        <p class="lead">A dark, elegant theme ecosystem for developers</p>
        <p class="description">
          Designed with focus and readability in mind, Flow provides a consistent experience 
          across editors, IDEs, and terminal emulators with carefully balanced colors.
        </p>
        <div class="cta-buttons">
          <a href="#platforms" class="btn">Get Started</a>
          <a href="https://github.com/Rics-Dev/flow" class="btn btn-secondary">GitHub</a>
        </div>
      </div>
      <div class="hero-image">
        <img src="/screenshots/neovim-preview.png" alt="Flow Theme Preview in Neovim" />
      </div>
    </div>
  </section>

  <section id="about" class="section">
    <div class="container">
      <h2>A theme crafted for developers</h2>
      <div class="features-grid">
        <div class="feature-card">
          <h3>Optimal Contrast</h3>
          <p>Carefully balanced contrast between background and foreground elements reduces eye strain during long coding sessions.</p>
        </div>
        <div class="feature-card">
          <h3>Semantic Highlighting</h3>
          <p>Colors are assigned based on code semantics, making it easier to understand code structure at a glance.</p>
        </div>
        <div class="feature-card">
          <h3>Consistent Experience</h3>
          <p>The same color philosophy is applied across all platforms, providing a familiar experience no matter where you code.</p>
        </div>
        <div class="feature-card">
          <h3>Plugin Integrations</h3>
          <p>Full support for popular plugins and extensions, ensuring a cohesive look throughout your development environment.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="colors" class="section">
    <div class="container">
      <h2>Color Palette</h2>
      <p>The Flow theme is built around a carefully selected color palette that balances readability, aesthetics, and semantic meaning.</p>
      <ColorPalette />
    </div>
  </section>

  <section id="platforms" class="section">
    <div class="container">
      <h2>Available Platforms</h2>
      <p>Flow theme is available for your favorite editors, IDEs, and terminal emulators.</p>
      <PlatformShowcase />
    </div>
  </section>

  <section id="testimonials" class="section">
    <div class="container">
      <h2>What Developers Say</h2>
      <div class="testimonials-grid">
        <div class="testimonial-card">
          <p>"Flow has become my go-to theme for all my development environments. The consistent experience across my editor and terminal makes context switching much easier."</p>
          <div class="testimonial-author">
            <span class="name">Alex Chen</span>
            <span class="title">Full Stack Developer</span>
          </div>
        </div>
        <div class="testimonial-card">
          <p>"The semantic highlighting in Flow makes code so much more readable. After a long day of coding, my eyes don't feel nearly as strained as they used to."</p>
          <div class="testimonial-author">
            <span class="name">Sarah Johnson</span>
            <span class="title">Backend Engineer</span>
          </div>
        </div>
        <div class="testimonial-card">
          <p>"I've tried dozens of dark themes, but Flow strikes the perfect balance between contrast and comfort. It's become an essential part of my workflow."</p>
          <div class="testimonial-author">
            <span class="name">Michael Torres</span>
            <span class="title">Frontend Developer</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    padding: 6rem 0 4rem;
    position: relative;
    overflow: hidden;
  }

  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-content {
    z-index: 1;
  }

  .lead {
    font-size: 1.5rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
    color: var(--accent-blue);
  }

  .description {
    font-size: 1.125rem;
    margin-bottom: 2rem;
    max-width: 540px;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
  }

  .hero-image {
    position: relative;
  }

  .hero-image img {
    border-radius: 8px;
    box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.5);
    border: 1px solid var(--border-accent);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .feature-card {
    background-color: var(--bg-secondary);
    border-radius: 8px;
    padding: 2rem;
    border: 1px solid var(--border-color);
    transition: transform 0.3s, border-color 0.3s;
  }

  .feature-card:hover {
    transform: translateY(-5px);
    border-color: var(--border-accent);
  }

  .feature-card h3 {
    color: var(--accent-blue);
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .testimonial-card {
    background-color: var(--bg-secondary);
    border-radius: 8px;
    padding: 2rem;
    border: 1px solid var(--border-color);
    position: relative;
  }

  .testimonial-card::before {
    content: """;
    position: absolute;
    top: 1rem;
    left: 1rem;
    font-size: 4rem;
    color: var(--accent-blue);
    opacity: 0.2;
    line-height: 0;
  }

  .testimonial-card p {
    margin-top: 1rem;
    position: relative;
    z-index: 1;
  }

  .testimonial-author {
    margin-top: 1.5rem;
    display: flex;
    flex-direction: column;
  }

  .testimonial-author .name {
    font-weight: 600;
    color: var(--accent-yellow);
  }

  .testimonial-author .title {
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  @media (max-width: 992px) {
    .hero .container {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-image {
      order: -1;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 4rem 0 3rem;
    }

    .lead {
      font-size: 1.25rem;
    }

    .description {
      font-size: 1rem;
    }
  }
</style>
